<UserControl x:Class="MeshhessenClient.Views.MeshCoreNodeList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="MeshCore Nodes" FontSize="18" FontWeight="SemiBold" Margin="8"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Nodes}" Margin="4">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding PublicKeyHex}" FontSize="10" Opacity="0.65"/>
                            <TextBlock Text="{Binding LastSeenUtc, StringFormat=Last seen: {0:g}}" FontSize="11" Opacity="0.75"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding Latitude, StringFormat=Lat: {0:F5}}" FontSize="10"/>
                            <TextBlock Text="{Binding Longitude, StringFormat=Lon: {0:F5}}" FontSize="10"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
